//! `EnglishSpecificCaseRule` (`EN_SPECIFIC_CASE`): the shared
//! [`crate::specific_case::SpecificCaseRule`] with the English strings and
//! `en/words/specific_case.txt` (the default `from_data` config).

pub(crate) use crate::specific_case::SpecificCaseRule;
